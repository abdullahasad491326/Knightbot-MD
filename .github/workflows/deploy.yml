name: Deploy Knightbot‑MD

on:
  push:
    branches:
      - main  # Or replace with your main branch

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'  # or a version compatible with Knightbot

      - name: Install dependencies
        run: npm install

      - name: Start bot
        run: nohup node index.js > bot.log 2>&1 &
